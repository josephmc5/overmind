start on (runlevel [2345] and stopped networking RESULT=ok)
stop on runlevel [!2345]

respawn

exec su -s /bin/sh -c 'exec "$0" "$@"' overmind -- {{ venv_root }}/bin/python {{ project_root}}/overmind/manage.py celeryd -l info

